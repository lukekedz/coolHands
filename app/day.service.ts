import { Injectable } from '@angular/core';

import { Day } from './day';

@Injectable()
export class DayService {
	// getDays(): Promise<Day[]> {
	// 	return firebase.database().ref('/2016/11')
	// 		.once("value").then(function(snapshot) {
	// 			console.log(snapshot.exportVal());
	// 			return snapshot.exportVal();
	// 		})
	// }
}
